<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="product">
    <!-- 01. 상품 전체 목록 -->
    <select id="carList" resultType="car">
        SELECT  
            car_id AS carId,
            car_type AS carType,
            fuel_type AS fuelType,
            car_fuel AS carFuel,
            car_name AS carName,
            car_price AS carPrice,
            brand_id AS brandId
        FROM car
        ORDER BY CAR_NAME
    </select>
    
    <!-- 02. 상품 상세보기 -->
    <select id="carDetail" resultType="car">
        SELECT  
            car_id AS carId,
            car_type AS carType,
            fuel_type AS fuelType,
            car_fuel AS carFuel,
            car_name AS carName,
            car_price AS carPrice,
            brand_id AS brandId
        FROM car
        WHERE car_id = #{car_id}
    </select>
</mapper>